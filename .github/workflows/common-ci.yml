env:
  RESOURCE_PATH: ./common/src/test/resources/application.yml
  S3_BUCKET_NAME: mcjserver
  CODE_DEPLOY_APPLICATION_NAME: title-code-deploy
  CODE_DEPLOY_DEPLOYMENT_GROUP_NAME: title-server

name: PR

on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, edited ]

permissions:
  contents: read

jobs:
  COMMON-Test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v1
        with:
          java-version: 17

      - name: "COMMON Test application.yml Set"
        uses: microsoft/variable-substitution@v1
        with:
          files: ${{ env.RESOURCE_PATH }}
        env:
          spring.datasource.url: ${{ secrets.RDB_TEST_URL }}
          spring.datasource.username: ${{ secrets.RDB_USER_NAME }}
          spring.datasource.password: ${{ secrets.RDB_PASSWORD }}

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew
        shell: bash

      - name: Api Test
        run: ./gradlew common:test --parallel --max-workers=4 --fail-fast
        shell: bash
