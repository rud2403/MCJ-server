spring:
  mail:
    host: ${mail.host:smtp.gmail.com}
    port: 587
    default-encoding: utf-8
    username: ${mail.username:mcj}
    password: ${mail.password:1234}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  thymeleaf:
    prefix: classpath:/templates/
    cache: false
